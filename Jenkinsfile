pipeline {
    agent 
    { 
        dockerfile true 
        label 'docker'
    }
    stages {
        stage('Test') {
            steps {
                echo 'App build'
            }
        }
    }
}
